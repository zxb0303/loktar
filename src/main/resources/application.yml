spring:
  application:
    name: loktar
  profiles:
    active: pro
server:
  port: 8080
  servlet:
    context-path: /loktar
mybatis:
  configuration:
    map-underscore-to-camel-case: true
  mapperLocations: classpath:com/loktar/mapper/**/xml/*.xml

